#include <stdio.h>
/**
 * main - main starts here
 * Return: returns success 0
 */
int main(void)
{
	char alpha;
	char BET;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	for (BET = 'A'; alpha <= 'Z'; BET++)
	{
		putchar(BET);
	}
		putchar('\n');
		return (0);
}
