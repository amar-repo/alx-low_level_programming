#include <stdio.h>
/**
 * main - Program starts here
 *
 * Return: Program exits with 0
 */
int main(void)
{
	char alpha;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
		putchar(alpha);
	}
		putchar('\n');
		return (0);
}
